/**
 * Legacy Page - Retained for route compatibility
 * Full-screen application view
 */
import { ThreadApp } from '@components/thread';

export function Thread() {
  return <ThreadApp />;
}

export default Thread;
