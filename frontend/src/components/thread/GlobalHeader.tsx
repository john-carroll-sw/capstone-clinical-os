/**
 * GlobalHeader Component (Legacy - header now in AppShell)
 */

export function GlobalHeader() {
  return (
    <header>
      <p>Header functionality moved to AppShell</p>
    </header>
  );
}

export default GlobalHeader;
