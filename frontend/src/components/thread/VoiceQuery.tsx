/**
 * VoiceQuery Component (Legacy - voice input now in AIChatPanel)
 * Keeping for backwards compatibility
 */

export function VoiceQuery() {
  return (
    <div>
      <p>Voice query functionality is now in AIChatPanel</p>
    </div>
  );
}

export default VoiceQuery;
